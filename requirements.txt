# Aletheia requirements.txt
#
# Python packages required to run inference and training, pinned to the exact
# versions used in the training environment (torch 2.13 + CUDA 13).
#
# - Inference (run.py) only needs: numpy, torch
# - Training (train.py) additionally needs: torchvision, scikit-image,
#   lpips, matplotlib, tqdm
#
# The complete raw `pip freeze` of the training environment is kept in
# requirements-full.txt (it contains Ubuntu system packages such as
# python-apt and PyGObject that cannot be pip-installed).

certifi==2025.1.31
filelock==3.32.2
fsspec==2026.7.0
idna==3.10
ImageIO==2.37.4
Jinja2==3.1.6
lazy-loader==0.5
lpips==0.1.4
MarkupSafe==3.0.3
matplotlib==3.10.3
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.2
packaging==25.0
pillow==12.3.0
psutil==7.0.0
requests==2.32.3
scikit-image==0.26.0
scipy==1.18.0
setuptools==84.0.0
sympy==1.14.0
tifffile==2026.7.31
torch==2.13.0
torchvision==0.28.0
tqdm==4.70.0
typing_extensions==4.16.0
urllib3==2.3.0
wheel==0.46.1